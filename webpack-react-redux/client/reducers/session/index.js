const initialState = {
    userId: 0,
    spaceId: 0,
    boardId: 0,
};

export default (state = initialState, action) => {
    switch (action.type) {
        default: {
            return state;
        }
    }
};
